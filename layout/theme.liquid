<!DOCTYPE html>
<!-- Last Published: {{ "now" | date: "%Y-%m-%dT%H:%M:%S.%L%z" }} -->
<html data-wf-page="{{ wfPage }}" data-wf-site="665ab0eb095a22df26e66257" lang="{{ request.locale.iso_code }}" data-money-format="{{ shop.money_format | escape }}"><head>{%- liquid
    if headerSnippetPath
        include headerSnippetPath
    else
        render "__default-head"
    endif
-%}
{{content_for_header}}
{% if template contains 'customers' %}
    {{ "shopify_common.js" | shopify_asset_url | script_tag }}
{% endif %}
</head>
<body data-w-id="665ab0eb095a22df26e66263" class="{{ bodyClasses }} {{ "template-" | append: request.page_type | handle }}">
{% if headerSnippetPath %}
    {{ content_for_layout }}
{% else %}
    {% include "backup-layout" %}
{% endif %}

{% include 'seal-subscriptions' %}
<script src="https://cdn-app.sealsubscriptions.com/shopify/js/seal-subscriptions.js" defer></script>
</body></html>